name: FoodOrder Docker CI

on:
  push:
    branches:
      - main

jobs:

  docker-build:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout source code
      uses: actions/checkout@v4

    - name: Build Docker Image
      run: docker build -t foodorder-app .

    - name: Check Docker Image
      run: docker images